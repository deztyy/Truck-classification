# --- โหมดเทสด้วยไฟล์วิดีโอ (Video Loop) ---
# Path ภายใน Container (ห้ามแก้บรรทัดนี้)
RTSP_URL=/app/video.mp4
PLAY_LOOP=true
# ชื่อไฟล์วิดีโอจริง บนเครื่องคอมของคุณ (ต้องวางไว้ที่เดียวกับ docker-compose.yml)
HOST_VIDEO_FILE=./test_video.mp4

# --- โหมดกล้องจริง (ถ้าจะใช้ให้เปิด comment นี้ แล้วปิดข้างบน) ---
# RTSP_URL=rtsp://admin:pass@192.168.1.55:554/stream
# PLAY_LOOP=false
# HOST_VIDEO_FILE=./test_video.mp4

# --- System Config ---
REDIS_HOST=redis
REDIS_PORT=6379
QUEUE_NAME=video_frames_queue
SHARED_PATH=/dev/shm
IMG_SIZE=640
MAX_QUEUE_SIZE=50
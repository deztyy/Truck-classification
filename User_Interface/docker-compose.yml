version: '3.8'
services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - DB_HOST=host.docker.internal # ใช้ตัวนี้เพื่อให้ Docker คุยกับ Postgres ในเครื่อง (localhost) ได้
    volumes:
      - .:/app